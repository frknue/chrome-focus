{
  "name": "chromium-focus",
  "version": "1.0.0",
  "description": "A Chrome extension to block domains with an easy-to-use interface",
  "scripts": {
    "build": "npm run clean && npm run copy",
    "clean": "rm -rf dist/*",
    "copy": "cp -r src/* dist/ && cp src/manifest.json dist/",
    "lint": "eslint src/**/*.js",
    "lint:fix": "eslint src/**/*.js --fix",
    "format": "prettier --write \"src/**/*.{js,json,html,css}\"",
    "format:check": "prettier --check \"src/**/*.{js,json,html,css}\"",
    "dev": "npm run build && npm run watch",
    "watch": "nodemon --watch src --ext js,json,html,css --exec npm run build"
  },
  "keywords": [
    "chrome-extension",
    "domain-blocker",
    "productivity"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "nodemon": "^3.0.3",
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}